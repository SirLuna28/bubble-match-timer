import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
 appId: 'Space Bubble Match - LouJJStudios',
  appName: 'Bubble Match Timer',
  webDir: 'dist/public'
};

export default config;
